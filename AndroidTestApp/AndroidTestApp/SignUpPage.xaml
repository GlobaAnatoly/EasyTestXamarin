<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AndroidTestApp.SignUpPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Войти" Clicked="OnEnterButtonClicked"></ToolbarItem>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout VerticalOptions="StartAndExpand">
            <Label Text="ФИО" />
            <Entry x:Name="nameEntry" Placeholder="Введите ФИО">
            </Entry>
            <Label Text="Логин" />
            <Entry x:Name="loginEntry" Placeholder="Введите логин">
            </Entry>
            <Label Text="Пароль" />
            <Entry x:Name="passwordEntry" IsPassword="true" >
            </Entry>
            <Label Text="Подтвердите пароль" />
            <Entry x:Name="passwordConfirmEntry" IsPassword="true" >
            </Entry>
            <Button Text="Зарегистрироваться" Clicked="OnRegisterButtonClicked" />
            <Label x:Name="messageLabel" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>